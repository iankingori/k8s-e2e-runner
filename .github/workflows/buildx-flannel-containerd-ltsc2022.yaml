name: Flannel Containerd LTSC 2022

on:
  push:
    branches:
      - main

jobs:
  buildx-flannel-containerd-ltsc2022:
    uses: ./.github/workflows/docker-buildx.yaml
    with:
      image_name: flannel-containerd-ltsc2022
      context: ./e2e-runner/e2e_runner/ci/capz_flannel/flannel/windows/containerd
      platform: windows/amd64
    secrets: inherit
